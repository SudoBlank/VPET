# VPet Sim (Python)

A desktop **virtual pet simulator** written in **Python from scratch**.  
Inspired by classic desktop pets like **VPet Sim** and **BongoCat**, but built as a fully local, and has an customizable application framwork.

## Running the Project

```bash
python app.py
```
OR (if the verson has one)
```bash
app.exe
```
